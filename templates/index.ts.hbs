{{#each children}}
import * as {{name.upperCamelCase}} from "./{{name.lowerCamelCase}}/{{name.lowerCamelCase}}";
{{/each}}
export {
{{#each children}}
    {{name.upperCamelCase}}{{#unless @last}},{{/unless}}
{{/each}}
}

{{!-- TODO: consider resolving the helpers as one --}}
{{!-- NOTE: helpers is deprecated since it uses shopperCustomer --}}
import * as helpers from "./helpers"
import * as slasHelper from "./slasHelper";

export {
    helpers,
    slasHelper
}

export { ClientConfig, CommonParameters, sdkLogger } from "@commerce-apps/core"
