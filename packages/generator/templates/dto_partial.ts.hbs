{{#each (onlyAdditional typeDto.properties)}}
    { [key: string]: {{{getDataType .}}} } &
{{/each}} {
    {{#each (onlyRequired typeDto.properties)}}
        {{{getValue name}}}: {{{getDataType .}}};
    {{/each}}
    {{#each (onlyOptional typeDto.properties)}}
        {{{getValue name}}}?: {{{getDataType .}}};
    {{/each}}
}
