
import { StaticClient, BaseClient, ClientConfig, ResponseError, AuthSchemes } from "@commerce-apps/core";
import _ from "lodash";


import {
{{#each dataTypes}}
    {{#if (isTypeDefinition .)}}
        {{getValue name}},
    {{/if}}
{{/each}}
} from "./{{apiSpec}}.types";


export class Client extends BaseClient {
  constructor(config: ClientConfig) {
    super(config);

    if (!!!config.baseUri) {
      config.baseUri = "{{servers/0/url}}";
    }
    {{#each models}}
        {{#each declares}}
            {{#is ./$classData/name "amf.client.model.domain.SecurityScheme"}}
                this.authSchemes["{{./name}}"] = new AuthSchemes["{{./name}}"]();
            {{/is}}
        {{/each}}
    {{/each}}
  }

{{#each models}}
    {{> operations model=. }}
{{/each}}

}

export default Client;
export { ClientConfig };
