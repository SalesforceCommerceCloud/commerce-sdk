
import { StaticClient, BaseClient, ClientConfig, ResponseError, AuthSchemes } from "@commerce-apps/core";
import _ from "lodash";

import {
{{#each dataTypes}}
    {{#if (isTypeDefinition .)}}
        {{getValue name}},
    {{/if}}
{{/each}}
} from "./{{apiSpec}}.types";

export {
{{#each dataTypes}}
    {{#if (isTypeDefinition .)}}
        {{getValue name}},
    {{/if}}
{{/each}}
} from "./{{apiSpec}}.types";


export class Client extends BaseClient {
  constructor(config: ClientConfig) {
    super(config);

    if (!!!config.baseUri) {
      config.baseUri = "{{getBaseUri models.[0] }}";
    }
    {{#each models}}
        {{#each declares}}
            {{#is ./$classData/name "amf.client.model.domain.SecurityScheme"}}
                this.authSchemes["{{./name}}"] = new AuthSchemes["{{./name}}"]();
            {{/is}}
        {{/each}}
    {{/each}}
  }

{{#each models}}
    {{> operations model=. }}
{{/each}}

}

export default Client;
export { ClientConfig };
