import { BaseClient } from "./core"

export default class extends BaseClient {
    constructor() {
        super("{{servers/0/url}}");
    }

{{#each endPoints}}
{{#each operations}}
    {{name}}() {
        this.{{method}}("{{../path}}");
    }

{{/each}}
{{/each}}
}

