{
    "extends": "../../base.tsconfig.json",
    "compilerOptions": {
        "outDir": "./dist"
    },
    // NOTE: Everything except what you want compiled into JS should be in the exclusions.
    "exclude": [
        "node_modules",
        "release",
        "test",
        "gulpfile.ts"
    ],
    "include": [
        "src"
    ]
}
