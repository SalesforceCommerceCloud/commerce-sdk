#%RAML 1.0 Trait

usage: |
  To be applied to the **response** on endpoints that can send back a 403 - Access Forbidden exception.

responses:
  401:
    description: |
      The caller is not authorized to access the requested resource.
    body:
      application/problem+json:
        type: ApiStandards.ErrorResponse
        example: !include ../examples/error/unauthorized-example.raml