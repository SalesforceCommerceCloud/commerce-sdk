The Price Adjustment without Promotion and Coupon references - handled as `AdHocPriceAdjustments` internally. 

The following fields are set:
- manual = true
- custom = true
- promotionId = AD_HOC_PRICE_ADJUSTMENT